FROM python:3.9-slim

# DEPRECATED: This Dockerfile is not used by the CI/workflows.
# The project uses local `sqlfluff` installation in CI steps.

RUN pip install sqlfluff==2.3.5

WORKDIR /scripts
ENTRYPOINT ["sqlfluff"]
CMD ["lint"]
