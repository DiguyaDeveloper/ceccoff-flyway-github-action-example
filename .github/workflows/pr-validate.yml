name: 🔍 SQL Validation

on:
  pull_request:
    branches: [ main ]
    paths: [ "sql/**" ]
  workflow_dispatch:

jobs:
  validate-changes:
    name: 📝 Validar Alterações SQL
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔍 Verificar Estrutura
        run: |
          if [ ! -d "sql" ]; then
            echo "::error::Diretório sql/ não encontrado"
            exit 1
          fi

          for dir in ddl dml repeatable; do
            if [ ! -d "sql/$dir" ]; then
              echo "::warning::Diretório sql/$dir não encontrado"
            fi
          done

      - name: 🔎 Identificar Arquivos
        id: changed-files
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            FILES=$(git diff --name-only origin/${{ github.base_ref }}... | grep '^sql/.*\.sql$' || true)
          elif [ -n "${{ inputs.pr_number }}" ]; then
            git fetch origin pull/${{ inputs.pr_number }}/head:pr-${{ inputs.pr_number }}
            git checkout pr-${{ inputs.pr_number }}
            FILES=$(git diff --name-only origin/main... | grep '^sql/.*\.sql$' || true)
          else
            FILES=$(find sql -type f -name "*.sql")
          fi
          echo "files=${FILES}" >> $GITHUB_OUTPUT

      - name: 📝 Validar Sintaxe SQL
        if: steps.changed-files.outputs.files != ''
        run: |
          echo "::group::Validação de Scripts SQL"
          for file in ${{ steps.changed-files.outputs.files }}; do
            echo "🔎 Analisando $file"
            if [ ! -r "$file" ]; then
              echo "::error file=$file::Arquivo não pode ser lido"
              exit 1
            fi
          done
          echo "::endgroup::"

      - name: 🔍 Validar Nomenclatura
        if: steps.changed-files.outputs.files != ''
        run: |
          echo "::group::Validação de Nomenclatura"
          while IFS= read -r file; do
            if [[ -f "$file" ]]; then
              echo "🔍 Verificando $file"
              basename=$(basename "$file")

              if [[ $file == sql/repeatable/* ]] && [[ ! $basename =~ ^R__ ]]; then
                echo "::error file=$file::Arquivo não segue o padrão R__"
                exit 1
              fi

              if [[ $file =~ sql/(ddl|dml)/* ]] && [[ ! $basename =~ ^V[0-9]{14}__ ]]; then
                echo "::error file=$file::Arquivo não segue o padrão V{YYYYMMDDHHMMSS}__"
                exit 1
              fi

              echo "✅ $file: nomenclatura válida"
            fi
          done <<< "${{ steps.changed-files.outputs.files }}"
          echo "::endgroup::"

      - name: 📊 Resumo
        if: always()
        run: |
          if [ -z "${{ steps.changed-files.outputs.files }}" ]; then
            echo "ℹ️ Nenhum arquivo SQL alterado"
          else
            echo "✅ Validação concluída com sucesso"
          fi
