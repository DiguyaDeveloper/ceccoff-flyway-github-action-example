name: 🗄️ Database Migration Workflow (dev/hml/prod)

on:
  push:
    branches: [ main ]
    paths: [ "sql/**" ]
  workflow_dispatch:
    inputs:
      environment:
        description: '🌍 Selecione o Ambiente'
        required: true
        default: 'dev'
        type: choice
        options: [dev, hml, prod]
      command:
        description: '⚡ Comando Flyway'
        required: true
        default: 'migrate'
        type: choice
        options: [info, validate, migrate, repair]


jobs:
  validate_prod:
    name: 🔍 Validação de Scripts (prod)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Carregar Flyway version
        run: |
          if [ -f .flyway-version ]; then
            echo "FLYWAY_VERSION=$(cat .flyway-version)" >> $GITHUB_ENV
          else
            echo "FLYWAY_VERSION=10.18.2" >> $GITHUB_ENV
          fi

      - name: 🔍 Verificar Estrutura
        run: |
          if [ ! -d "sql" ]; then
            echo "::error::Diretório sql/ não encontrado"
            exit 1
          fi

          for dir in ddl dml repeatable; do
            if [ ! -d "sql/$dir" ]; then
              echo "::warning::Diretório sql/$dir não encontrado"
            fi
          done

      - name: 📝 Validar Sintaxe SQL com sqlfluff
        run: |
          pip install sqlfluff --break-system-packages
          echo "::group::Validação de Scripts SQL"
          find sql -type f -name "*.sql" -exec sqlfluff lint {} \;
          echo "::endgroup::"

      - name: 🔍 Validar Nomenclatura
        run: |
          echo "::group::Validação de Nomenclatura"

          error_count=0
          error_files=""

          # Validar scripts versionados
          while IFS= read -r file; do
            if [[ -f "$file" ]]; then
              basename=$(basename "$file")
              echo "🔎 Verificando: $file"

              if [[ ! $basename =~ ^V[0-9]{14}__ ]]; then
                echo "::error file=$file::❌ $basename - Não segue o padrão V{YYYYMMDDHHMMSS}__"
                error_count=$((error_count + 1))
                error_files="$error_files\n- $file"
              else
                echo "✅ $basename - OK"
              fi
            fi
          done < <(find sql/ddl sql/dml -type f -name "*.sql" 2>/dev/null)

          # Validar scripts repetíveis
          while IFS= read -r file; do
            if [[ -f "$file" ]]; then
              basename=$(basename "$file")
              echo "🔎 Verificando: $file"

              if [[ ! $basename =~ ^R__ ]]; then
                echo "::error file=$file::❌ $basename - Não segue o padrão R__"
                error_count=$((error_count + 1))
                error_files="$error_files\n- $file"
              else
                echo "✅ $basename - OK"
              fi
            fi
          done < <(find sql/repeatable -type f -name "*.sql" 2>/dev/null)

          if [ $error_count -gt 0 ]; then
            echo "❌ Encontrados $error_count arquivo(s) com erro de nomenclatura:"
            echo -e "$error_files"
            exit 1
          fi

          echo "✅ Todos os arquivos seguem o padrão de nomenclatura"
          echo "::endgroup::"

      - name: 📊 Flyway - Info pré-validação
        # Executa um "info" para listar migrations presentes no banco antes da validação.
        run: |
          docker run --rm \
            -v "${{ github.workspace }}:/workspace" \
            -w /workspace \
            -e FLYWAY_URL -e FLYWAY_USER -e FLYWAY_PASSWORD \
            flyway/flyway:${{ env.FLYWAY_VERSION }} \
            info

      - name: 🔍 Flyway - Validate (pré-aplicação)
        # Esta etapa garante que Flyway valide os scripts (checks de consistência) antes de permitir a
        # execução da migração no job de deploy. Se falhar, interrompe a pipeline.
        run: |
          set -e
          docker run --rm \
            -v "${{ github.workspace }}:/workspace" \
            -w /workspace \
            -e FLYWAY_URL -e FLYWAY_USER -e FLYWAY_PASSWORD \
            flyway/flyway:${{ env.FLYWAY_VERSION }} \
            validate

  approval_prod:
    name: ✋ Aprovação Manual (prod)
    needs: validate_prod
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment || 'dev' }}
    if: inputs.environment != 'dev'
    steps:
      - run: echo "✅ Migração aprovada para ${{ inputs.environment }}"
  migrate_prod:
    name: 🚀 Execução da Migração (prod)
    needs: [validate_prod, approval_prod]
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment || 'dev' }}
    timeout-minutes: 30

    concurrency:
      group: flyway-${{ inputs.environment || 'dev' }}
      cancel-in-progress: false

    steps:
      - uses: actions/checkout@v4

      - name: Carregar Flyway version
        run: |
          if [ -f .flyway-version ]; then
            echo "FLYWAY_VERSION=$(cat .flyway-version)" >> $GITHUB_ENV
          else
            echo "FLYWAY_VERSION=10.18.2" >> $GITHUB_ENV
          fi

      - name: Configurar Variáveis
        run: |
          echo "FLYWAY_URL=${{ secrets.DB_URL }}" >> $GITHUB_ENV
          echo "FLYWAY_USER=${{ secrets.DB_USER }}" >> $GITHUB_ENV
          echo "FLYWAY_PASSWORD=${{ secrets.DB_PASS }}" >> $GITHUB_ENV

      - name: ⚡ Executar Flyway
        id: migrate
        run: |
          docker run --rm \
            -v "${{ github.workspace }}:/workspace" \
            -w /workspace \
            -e FLYWAY_URL -e FLYWAY_USER -e FLYWAY_PASSWORD \
            flyway/flyway:${{ env.FLYWAY_VERSION }} \
            ${{ inputs.command || 'migrate' }}

      - name: 📈 Info Pós-Migração
        if: always()
        run: |
          docker run --rm \
            -v "${{ github.workspace }}:/workspace" \
            -w /workspace \
            -e FLYWAY_URL -e FLYWAY_USER -e FLYWAY_PASSWORD \
            flyway/flyway:${{ env.FLYWAY_VERSION }} \
            info

      - name: 📣 Notificar Status
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const notify = require('./.github/scripts/notify.js');
            await notify({
              github,
              context,
              status: "${{ job.status }}",
              environment: "${{ inputs.environment || 'dev' }}",
              command: "${{ inputs.command || 'migrate' }}"
            });
